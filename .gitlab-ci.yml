--- 
cache: 
  paths: 
    - node_modules/
image: "node:latest"
before_script:
    stage: setup, test, build
      - cd backend
install_dependencies: 
  stage: setup
  artifacts: 
    paths: 
      - node_modules/
  script: 
    - "npm install"
execute_tests: 
  stage: test
  script: "npm test -- --updateSnapshot --passWithNoTests"
build_project:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/
stages:
  - setup
  - test
  - build

